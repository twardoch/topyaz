# Local preview of the topyaz docs site:
#   cd docs && bundle install && bundle exec jekyll serve
# GitHub Pages builds with the remote Just the Docs theme; this Gemfile is for local runs only.
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.10"
gem "jekyll-remote-theme"
